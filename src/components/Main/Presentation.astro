<div
    class="flex min-h-screen flex-col justify-center gap-8 text-lg md:text-xl"
    id="home"
>
    <span>Hola, soy</span>
    <h1 class="font-lexend pb-3 font-bold">
        Isaac
        <br />
        Rojas
    </h1>
    <span class="leading-8">
        Bachiller en Ingeniería Electrónica
        <br />
        Desarrollador Web Full-Stack
    </span>
</div>

<style>
    h1 {
        background: linear-gradient(
            var(--h1-angle, 135deg),
            hsl(var(--h1-grad-from)),
            hsl(var(--h1-grad-to))
        );
        background-clip: text;
        -webkit-background-clip: text;
        color: transparent;
        animation: angle-shift 6s ease-in-out infinite alternate;
    }

    @keyframes angle-shift {
        0% {
            --h1-angle: 5deg;
        }
        45% {
            --h1-angle: 365deg;
        }
        75% {
            --h1-angle: 100deg;
        }
        100% {
            --h1-angle: 365deg;
        }
    }

    @property --h1-angle {
        syntax: "<angle>";
        inherits: false;
        initial-value: 5deg;
    }
</style>
