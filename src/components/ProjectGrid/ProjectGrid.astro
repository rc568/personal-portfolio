---
import { projects } from "../../utils/info";
import ProjectCard from "./ProjectCard.astro";
---
<section class="pb-12">
    <h2 class="text-xl pb-4">Proyectos</h2>
    <div class="grid grid-cols-2 gap-4 justify-between max-md:grid-cols-1">
        {projects.map((project) => <ProjectCard {...project} />)}
    </div>
</section>
